# Required Environment Variables
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration (Required for Balance Agent)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Google AI API Configuration (Required for Orchestrator Agent)
# Get your API key from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Optional Configuration

# OpenAI Model Selection (defaults to "gpt-4o-mini")
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, etc.
OPENAI_MODEL=gpt-4o-mini

# Server Port Configuration
# Port for the main API server (defaults to 8000)
AGENTS_PORT=8000

# External URL Configuration
# Used for agent card endpoints (defaults to http://localhost:{AGENTS_PORT})
# Set this if deploying to production or using a different domain
# RENDER_EXTERNAL_URL=http://localhost:8000

# Balance Agent Port (optional)
# Port for standalone balance agent server (defaults to 9001)
# ITINERARY_PORT=9001
